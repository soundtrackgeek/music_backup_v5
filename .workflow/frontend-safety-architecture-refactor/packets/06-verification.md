# Packet 06: Verification and Git follow-through

Objective: run the full release gate, audit the integrated diff, clean Rust artifacts, commit, and push.

Ownership: no product behavior changes except evidence-driven fixes.

Verification: `npm run check`, workflow verifier, final Git status, `cargo clean`, commit, and upstream push.
