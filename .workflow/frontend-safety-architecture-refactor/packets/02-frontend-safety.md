# Packet 02: Frontend safety

Objective: add Vitest/Testing Library and focused behavior tests before structural extraction.

Ownership: package test setup, request/settings/navigation/review-state helpers and tests.

Verification: `npm run test:run` and `npm run build`.
